# T128 — RED evidence

- Gate: `GATE-SWARM-001`
- Command:
  `node --test --test-name-pattern='GATE-SWARM-001.*planned RED E2E' scripts/swarm.test.mjs`
- Exit code: `1`
- Result: `1 failed`.
- Missing lifecycle contract: both the E2E prompt and agent configuration treated the committed
  T080–T085 RED block as an unexpected baseline failure.
- Missing propagation contract: an output beginning with `` `REQUEST_CHANGES` `` returned exit
  `0`.
- Product and frozen contracts changed before RED: no.
