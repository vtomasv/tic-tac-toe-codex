# T113 — RED evidence

- Gate: `GATE-SWARM-001`
- Date: `2026-07-23`
- Command: `node --test --test-name-pattern='GATE-SWARM-001.*nounset|GATE-SWARM-001.*supported non-interactive' scripts/swarm.test.mjs`
- Exit code: `1`
- Compatibility test: supported non-interactive Codex CLI options passed.
- Failing test: `GATE-SWARM-001 rejects nounset-unsafe dependent local declarations`.
- First failure: `mk_wt` initialized `role`, `branch` and `path` in one `local` statement, so `${role}` was expanded while unset under `set -u`.

