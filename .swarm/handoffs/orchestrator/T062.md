# T062 — RED evidence for GATE-MULTIFEATURE-001

- Date: 2026-07-22
- Commit: `587accca01f3fc9675d72bd9262a3827ae29800d`
- Command: `node --test --test-name-pattern='GATE-MULTIFEATURE-001' scripts/verify-traceability.test.mjs`
- Exit code: `1`
- Result: `11` tests, `0` passed, `11` failed.
- First expected failure: the inactive `002-second` fixture contained an unknown AC reference, but
  the active-only verifier returned exit code `0` (`Expected 1, actual 0`).
- Missing behavior demonstrated: multi-feature discovery, global AC/GATE/Task uniqueness, union of
  tasks for `git log`, partial-feature diagnostics, ledger lifecycle rules, deterministic
  feature-prefixed errors, release-maturity evidence rules, repository aggregation, and
  supplemental RED/GREEN pair representation.
- Stop-condition audit: the test failed because the gate capabilities are absent; it did not fail
  because of fixture setup, dependency installation, or feature 001 behavior.
