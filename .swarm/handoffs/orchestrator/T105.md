# T105 — RED evidence

- Gate: `GATE-SWARM-001`
- Date: `2026-07-23`
- Command: `node --test --test-name-pattern='GATE-SWARM-001.*self-contained|GATE-SWARM-001.*versioned root' scripts/swarm.test.mjs`
- Exit code: `1`
- Passing compatibility test: versioned `.prompts/` root.
- Failing test: `GATE-SWARM-001 prepares self-contained worktrees with explicit boundaries`.
- First failure: `scripts/swarm.sh` did not contain `link_dependencies()`.
- Cause: new worktrees had no access to the ignored root `node_modules`, and the prompts named an undefined boundary gate.

