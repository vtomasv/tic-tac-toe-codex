# T124 — RED evidence

- Gate: `GATE-SWARM-001`
- Command:
  `node --test --test-name-pattern='GATE-SWARM-001.*browser-capable E2E' scripts/swarm.test.mjs`
- Exit code: `1`
- Result: `1 failed`.
- Missing contract 1: `launch-e2e` still passed `workspace-write`, which prevented Playwright
  Chromium from registering its Mach rendezvous service.
- Missing contract 2: an `e2e.out` beginning with `REQUEST_CHANGES` returned exit `0`.
- Domain/interfaz sandbox contract: both remained `workspace-write`.
- Runner changed before RED: no.
