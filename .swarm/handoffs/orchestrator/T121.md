# T121 — RED evidence

- Gate: `GATE-SWARM-001`
- Command: `node --test --test-name-pattern='GATE-SWARM-001.*unblocked handoff' scripts/swarm.test.mjs`
- Exit code: `1`
- Result: `1 failed`.
- First cause: `reject_blocked_handoff` returned the non-match exit code from `grep` when
  `domain.out` contained `READY_FOR_ORCHESTRATOR`; under `set -e`, the successful fan-out was
  therefore reported as a launcher failure.
- Production script changed before RED: no.
