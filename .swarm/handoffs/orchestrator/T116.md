# T116 — RED evidence

- Gate: `GATE-SWARM-001`
- Date: `2026-07-23`
- Command: `node --test --test-name-pattern='GATE-SWARM-001.*linked worktree|GATE-SWARM-001.*nounset' scripts/swarm.test.mjs`
- Exit code: `1`.
- Compatibility test: nounset-safe initialization passed.
- Failing test: `GATE-SWARM-001 grants linked worktree writes and propagates blocked handoffs`.
- First failure: `codex exec` did not add the common `$ROOT/.git` as a writable directory.
- Observed fan-out evidence: both agents returned `REQUEST_ORCHESTRATOR`; Git could not create the linked-worktree `index.lock`, and domain also reported Vite unable to write `.vite-temp` through the dependency link.

