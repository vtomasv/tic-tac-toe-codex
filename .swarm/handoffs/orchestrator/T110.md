# T110 — RED evidence

- Gate: `GATE-SWARM-001`
- Date: `2026-07-23`
- Command: `node --test --test-name-pattern='GATE-SWARM-001.*supported non-interactive|GATE-SWARM-001.*self-contained' scripts/swarm.test.mjs`
- Exit code: `1`
- Compatibility test: self-contained worktrees passed.
- Failing test: `GATE-SWARM-001 uses supported non-interactive Codex CLI options`.
- First failure: the runner still contained unsupported `--ask-for-approval`.
- CLI evidence: `codex exec --help` for 0.145.0 exposes `-c` and `--sandbox`, but not `--ask-for-approval`.

