# T096 — GREEN del contrato compatible de anuncios de GameStatus

- AC: `AC-US5-A11Y-029`, `AC-US5-A11Y-030`
- Archivo: `src/components/GameStatus.tsx`
- Comando filtrado:
  `npm run test:component -- --configLoader=runner --testNamePattern='AC-US5-A11Y-0(29|30)'`
- Resultado filtrado: PASS, 2 tests.
- Comando suite: `npm run test:component -- --configLoader=runner`
- Resultado suite: PASS, 3 archivos y 36 tests.
- Comando build: `npm run build`
- Resultado build: PASS con el `App.tsx` anterior, que continúa proporcionando solo `status`.
- Decisión de compatibilidad: `status` sigue requerido, `announcement` es opcional y el mismo nodo
  estable `role="status"` presenta `announcement` cuando está definido o el mensaje canónico en
  caso contrario.
