# T076 — RED de estados y nombre de UndoButton

- AC: `AC-US5-DISPONIBILIDAD-003`, `AC-US5-DISPONIBILIDAD-004`,
  `AC-US5-A11Y-028`, `AC-US5-VISUAL-034`
- Archivo: `src/components/UndoButton.test.tsx`
- Comando:
  `npm run test:component -- --configLoader=runner --testNamePattern='AC-US5-(DISPONIBILIDAD-00[3-4]|A11Y-028|VISUAL-034)'`
- Resultado RED: exit code 1. Vitest no pudo resolver `./UndoButton` porque el componente todavía no
  existe; `UndoButton.test.tsx` quedó como la única suite fallida y los 26 tests previos se omitieron.
- Nota operativa: `--configLoader=runner` evita que Vite intente escribir `.vite-temp` dentro del
  `node_modules` enlazado fuera del sandbox; no altera qué tests se ejecutan.
